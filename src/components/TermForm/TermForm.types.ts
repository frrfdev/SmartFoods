export interface TermFormProps {
  termId: string;
  title: string;
  description: string;
  image: any;
}

export interface TermFormErrors {
  termId?: string;
  title?: string;
  description?: string;
}
